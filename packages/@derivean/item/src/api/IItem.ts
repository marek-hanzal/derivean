export interface IItem {
    /**
     * Name of a item (like log, gem, gold or even more abstract things).
     */
    name: string;
    /**
     * Type of item; could be item, building, item, ...
     */
    type: string;
}
